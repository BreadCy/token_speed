# Rust 构建产物
tokenspeed-rs/target/

# 部署用的二进制（由 target/release 复制，不进版本库）
tokenspeed/bin/*.exe

# ZCode 会话/计划数据
.zcode/

# 构建产物
_dist_artifacts/
dist/

# 测试 fixture 被全局 gitignore 的 tokenspeed-rs/tests 规则误伤，
# 测试代码（collectors_tests/monitor_tests）依赖它们，必须进版本库
!tokenspeed-rs/tests/
